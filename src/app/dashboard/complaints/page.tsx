import { PlaceholderPage } from "@/components/dashboard/PlaceholderPage";

export default function ComplaintsPage() {
  return (
    <PlaceholderPage
      icon="flag"
      title="Complaints"
      description="Submit and track complaints to the SRC directly from your dashboard, with real-time status updates."
    />
  );
}
